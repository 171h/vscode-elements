name: Release

# `npm run release` creates and pushes a tag after updating the version and
# CHANGELOG. Pushing the tag publishes it; manual dispatch allows retries.
on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version_tag:
        description: 'Existing release tag to publish, for example v2.6.0'
        required: true
        type: string

permissions:
  contents: write
  id-token: write

concurrency:
  group: release-${{ inputs.version_tag || github.ref_name }}
  cancel-in-progress: false

jobs:
  release:
    if: github.event_name != 'push' || !github.event.deleted
    runs-on: ubuntu-latest
    env:
      VERSION_TAG: ${{ inputs.version_tag || github.ref_name }}
    steps:
      - name: Validate release tag
        shell: bash
        run: |
          if [[ ! "$VERSION_TAG" =~ ^v(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)(-[0-9A-Za-z.-]+)?(\+[0-9A-Za-z.-]+)?$ ]]; then
            echo "Invalid version tag: $VERSION_TAG" >&2
            echo "Expected a tag such as v2.6.0 or v2.6.0-beta.1" >&2
            exit 1
          fi

      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          ref: refs/tags/${{ env.VERSION_TAG }}
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          registry-url: 'https://registry.npmjs.org'
          cache: npm

      - name: Verify release version
        shell: bash
        run: |
          PACKAGE_VERSION=$(node -p "require('./package.json').version")
          TAG_VERSION=${VERSION_TAG#v}
          if [[ "$PACKAGE_VERSION" != "$TAG_VERSION" ]]; then
            echo "Tag version $TAG_VERSION does not match package version $PACKAGE_VERSION" >&2
            exit 1
          fi
          echo "PACKAGE_VERSION=$PACKAGE_VERSION" >> "$GITHUB_ENV"

      - name: Verify npm token is configured
        shell: bash
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          if [[ -z "$NODE_AUTH_TOKEN" ]]; then
            echo "Configure the NPM_TOKEN repository secret with npm publish access." >&2
            exit 1
          fi

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Test
        run: npm run test

      - name: Test release scripts
        run: npm run test:release

      - name: Lint
        run: npm run lint

      - name: Check formatting
        run: npm run prettier

      - name: Verify changelog and extract release notes
        run: node scripts/extract-release-notes.mjs "$PACKAGE_VERSION"

      - name: Pack package
        run: npm pack

      - name: Publish to npmjs
        shell: bash
        run: |
          if npm view "nusys-ui@${PACKAGE_VERSION}" version >/dev/null 2>&1; then
            echo "nusys-ui@${PACKAGE_VERSION} is already published; skipping npm publish."
          else
            npm publish "nusys-ui-${PACKAGE_VERSION}.tgz" --access=public --provenance
          fi
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          PACKAGE_FILE="nusys-ui-${PACKAGE_VERSION}.tgz"
          if gh release view "$VERSION_TAG" >/dev/null 2>&1; then
            gh release edit "$VERSION_TAG" \
              --title "$VERSION_TAG" \
              --notes-file dist/release-notes.md
            gh release upload "$VERSION_TAG" "$PACKAGE_FILE" --clobber
          else
            gh release create "$VERSION_TAG" "$PACKAGE_FILE" \
              --title "$VERSION_TAG" \
              --notes-file dist/release-notes.md
          fi
